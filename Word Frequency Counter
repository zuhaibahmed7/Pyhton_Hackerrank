text = "apple banana apple cherry banana apple"
lst = text.split()
frequency = {}
for word in lst:
    if word in frequency:
        frequency[word] +=1
    else:
        frequency[word] = 1
        
print(frequency)
